# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Models / Data / Checkpoints (大文件不进 git)
# 下载的模型权重 (魔搭/HF), 全部可再生, 绝不进 git
models/
phase0/checkpoints/
phase0/data/raw/
phase0/data/processed/
# phase1 demo CPT 语料 (脚本可再生, 和 phase0/data 一致)
phase1/data/
phase0/results/*.bin
phase0/results/*.pt
phase0/results/*.safetensors
# week12 评估: fused 模型可由 mlx_lm.fuse 重生 (1.4GB), 不进 git; 只 track scores/报告 json
phase1/results/week12_eval/week11_cpt_fused/
phase1/results/week12_eval/real_cpt_fused/
# week12 真数据评估日志 (progress bar 噪声, 瞬时)
phase1/results/week12_eval/run_real_eval.log
*.bin
*.pt
*.safetensors
*.pkl
*.npy
hf_cache/
~/.cache/huggingface/

# 实验产物
runs/
wandb/
mlruns/
lightning_logs/
outputs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 凭证
.env
.env.local
*.key

# 论文 PDF (arxiv 公开可重新下载, 大文件不进 git 历史)
**/papers/*.pdf

# OMC
.omc/state/
.omc/logs/
phase0-foundation-plan.md
llm-domain-adaptation-master-plan.md
/.omc
/.claude
phase1-deep-dive-plan.md
phase2-integration-plan.md
phase3-career-transition-plan.md
/phase0/.omc
/.playwright-mcp

# scratch / smoke / probe (训练验证产物, 不进 git; 真实结果目录仍追踪 JSON/config/loss_log)
phase1/results/week15_dpo_smoke*
phase1/results/**/_smoke*/
phase1/results/**/_smoke_qwen3/
phase1/results/**/_probe*/

# week16 失败模式: sweep log 是 progress bar 噪声 (照 week12 run_real_eval.log); detached pid 文件不进 git
# 真实结果 (loss_log/run_config/scores/*/JSON) 仍追踪, 与 week15_dpo 同口径; 大权重由全局 *.safetensors 挡掉
phase1/results/week16_failmode/sweep_run.log
phase1/results/week16_failmode/.*_pid
