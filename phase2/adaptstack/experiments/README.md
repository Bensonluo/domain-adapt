# Experiments

Use one file per hypothesis and record the immutable base configuration, data
snapshot, seed, changed variables, and primary metric. The template is metadata
for experiment review; applying dotted overrides is intentionally deferred until
the configuration-composition backend is selected.
