# HuatuoGPT-II 论文精读笔记

> 论文: HuatuoGPT-II, One-stage Training for Medical Chatbot (Chen et al., 2023)
> 链接: https://arxiv.org/abs/2311.06750
> 团队: 清华（小样本学习 + 医疗）

---

## 核心观点 (TL;DR)

```
医疗 LLM 的传统范式是「领域 CPT → 任务 SFT」两阶段。
HuatuoGPT-II 指出这个范式有个内在矛盾：
   CPT 用的是无格式纯文本续写，SFT 用的是指令对话 —— 两个阶段数据分布割裂，
   导致 SFT 要么"唤醒灾难性遗忘"、要么"知识学进去了但取不出来"。

解法：One-stage Training（单阶段训练）
   把所有数据（含 PubMed 医学知识）统一转成 ChatML 对话格式，
   在一个阶段内同时注入「领域知识 + 对话能力」。

一句话：CPT 的数据格式必须匹配你最终怎么用它（对话），
       否则知识是"会背但不会用"的死知识。
```

---

## ⚠️ 先澄清一个常见误区：两阶段是 HuatuoGPT-**I** 的范式

README / 很多博客把 "CPT + SFT 两阶段流程" 挂在 HuatuoGPT 名下。要分清：

```
HuatuoGPT (第一代):
  Stage 1: CPT  —— 混合「真实医生文本」+「ChatGPT 合成的医疗 QA」继续预训练
  Stage 2: RLHF —— 用医生专家给 reward 做人类偏好对齐
  特点: 经典两阶段，CPT(知识) 和 对齐(风格) 分开

HuatuoGPT-II (本篇):
  核心卖点 = 废掉两阶段，合成 One-stage
  为什么？因为发现两阶段里 CPT 和 SFT 的数据格式割裂，知识"流失"了
```

**记法**：I = 两阶段（CPT+RLHF）；II = 单阶段（One-stage）。II 不是 I 的"升级版数据"，是**范式级别的重构**。

---

## 两阶段范式的内在矛盾（II 要解决的核心问题）

```
传统两阶段医疗 LLM:

  Stage CPT:  「无格式纯医学文本」  →  next-token prediction
               "患者男性，54岁，主诉胸痛..."
               (知识密度高，但不是对话)

  Stage SFT:  「指令-回复对话」     →  学会回答问题
               Q: "胸痛怎么办？"  A: "建议立即就医..."
               (是对话，但知识密度低)

  问题: 两个阶段的「数据分布」完全不一样
        ─ CPT 阶段模型学会了"医学事实"，但状态是"续写文本"
        ─ SFT 阶段要把模型从"续写"拉到"对话"，这个过程会:
            ① 丢掉一部分 CPT 学的医学知识 (catastrophic forgetting)
            ② 或者知识还在，但"埋在权重里取不出来" (knowledge not elicited)

  结果: 医疗问答时，模型有时会"忘掉"专业内容，或答得很泛、不专业。
```

**本质**：CPT 学的知识"形态"（续写）和下游用的形态（对话）对不上。**学进去了 ≠ 用得出来**。

---

## One-stage Training 怎么做

```
思路: 既然问题是"格式割裂"，那就统一格式 —— 全部用 ChatML 对话格式。

  Step 1: 把「医学知识」转成对话
          PubMed 文献 / 医学文本  ──(prompting/转换)──>  ChatML 风格的多轮医疗 QA
          例: 一段关于"心梗治疗"的文献
               → 改写成 "问：心梗的急救流程？答：① 立即...② ..."

  Step 2: 统一训练目标
          所有数据(知识转换的QA + 原有指令数据) 都用 next-token prediction
          但都在 ChatML 模板里:
            <|im_start|>system\n你是一个专业医生...<|im_end|>
            <|im_start|>user\n{question}<|im_end|>
            <|im_start|>assistant\n{answer}<|im_end|>

  Step 3: 单阶段从头训到尾，CPT 和 SFT 不再分开

  效果: 模型在"学医学知识"的同时，始终处于"对话格式"，
        知识直接以可被调用的形式存进去，SFT 时不用再做格式转换 → 不流失。
```

### 为什么这样能避免"知识取不出来"

```
两阶段:  知识以「续写分布」存入 ──SFT 拉格式──> 分布漂移 ──> 知识丢失/埋没
单阶段:  知识直接以「对话分布」存入 ────────────> 下游就是对话 ──> 无缝调用

类比: 同样是"学法语医学词汇"
  两阶段 = 先默写词汇表(CPT)，再去练口语(SFT) —— 默写的词开口时想不起来
  单阶段 = 直接在对话里学 —— 学到的就是"能说出口的"那个形态
```

---

## 数据构造（II 的另一个工程亮点）

```
痛点: ChatML 格式的医疗 QA 从哪来？真实高质量医疗对话很稀缺。

II 的做法（数据工程）:
  1. 从 PubMed 等医学文献获取大量专业文本（知识来源）
  2. 用 LLM 把"文献段落"改写成"多轮医疗问答"（知识 → 对话化）
     —— 这一步本身是 synthetic data 生成（呼应 Phase1 后面 Week21 合成数据）
  3. Resort / 重组: 把单轮 QA 重组成多轮对话，增加上下文多样性
  4. 得到大量"知识密集 + 对话格式"的训练数据

价值: 把「无标注医学文献」这个巨大资产，转化成了「可训练的 ChatML 数据」。
      —— 这正是 Domain Adaptation 的核心杠杆: 榨干无标注领域文本。
```

---

## 评估

```
医疗 benchmark:
  - CMB (Comprehensive Medical Benchmark, 中文医疗综合评测)
  - MedQA / USMLE (美国执业医师考试)
  - NCLEX (护理考试)
  - 国内外执业/资格考试

评估方法:
  - 客观题 accuracy
  - 引入 LLM-as-judge (用更强模型给生成答案打分)
  - 部分人工评估 (专业医学合理性)

结果: 在多个医疗 benchmark 上超过同期的医疗 LLM（含两阶段方案）。
      核心证据 = "单阶段"在"知识调用类问题"上明显更好。
```

---

## 实战启示（我做 CPT 会怎么用）

1. **CPT 数据的格式 = 下游使用的格式**。如果你最终是做对话/助手，CPT 数据就别用纯文本续写，直接用 ChatML/instruction 格式。**这是这篇最大的工程启示。**
2. **无标注领域文本的杠杆**：PubMed/内部文档这种"只有文本没标注"的资产，可以用 LLM 转成 instruction 数据再做 CPT——别浪费。
3. **警惕"CPT 学了但 SFT 丢了"**：如果你发现 CPT 后模型在某些知识上变强、但 SFT 后又退化了，很可能就是格式割裂。考虑合并阶段。
4. **单阶段 ≠ 一定更好**：单阶段的前提是数据量足够、且转换质量高。数据少时两阶段（先 CPT 补知识再 SFT）可能更稳。这是 trade-off，不是银弹。

---

## 我的 takeaway

1. **"学进去 ≠ 用得出来"** —— 这是我从这篇学到的最重要认知。CPT 的效果不只看"学了多少知识"，更看"知识以什么形态存进去、能不能被下游格式调用"。
2. **格式一致性是被低估的设计维度**。之前我默认 CPT 就是"拿领域文本续写"，没想过格式问题。这篇让我以后做 CPT 第一件事就是问：**我最终怎么用它？CPT 数据格式对齐了吗？**
3. **和 Don't Stop Pretraining 互补**。DSP（2020）证明"CPT 有用"，HuatuoGPT-II（2023）指出"CPT 的数据格式也要讲究"。前者解决"要不要做"，后者解决"怎么做才不浪费"。
4. **数据工程前置**。II 一半的贡献在"把文献转成对话数据"这个工程上——呼应了 Phase1 "合成数据"主线，CPT 不只是训练问题，是数据问题。

---

## 待深入的问题

- [ ] 单阶段 vs 两阶段，在数据量很小（< 1B tokens）时哪个更稳？（我的直觉是两阶段，需验证）
- [ ] "文献 → ChatML QA" 的转换质量如何控制？转换噪声会不会污染 CPT？（合成数据质量 → Week21）
- [ ] One-stage 的学习率怎么设？它同时承担 CPT 和 SFT 两个目标的强度（→ concepts 笔记）
